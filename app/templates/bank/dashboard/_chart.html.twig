<div class="card bg-base-100 shadow-xl">
    <div class="card-body">
        <h2 class="card-title">{{ chart.title }}</h2>
        <div class="h-[300px]">
            {{ render_chart(chart.chart) }}
        </div>
    </div>
</div>