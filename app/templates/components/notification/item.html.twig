<div class="flex items-center gap-2">
    <div class="avatar">
        <div class="w-8 rounded-full">
            <img src="https://placehold.co/100x100" alt="avatar" />
        </div>
    </div>
    <div>
        <p class="text-sm">{{ notification.message }}</p>
        <p class="text-xs opacity-50">{{ notification.time }}</p>
    </div>
</div>